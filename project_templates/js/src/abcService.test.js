import { expect, test } from 'vitest'
import { pt1, pt2 } from './abcService.js'

test('Test input part 1', () => {
  const input = [
    '',
  ]

  expect(pt1(input)).toBe(0)
})

test('Test input part 2', () => {
  const input = [
    '',
  ]

  expect(pt2(input)).toBe(0)
})

